- routeTemplate:
    id: template-1
    parameters:
    - name: routeTemplateId
    - name: routeId
    - name: message
    from:
      uri: "timer://{{routeId}}"
      steps:
      - log: "{{message}}"
